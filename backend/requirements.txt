# MediTrace Backend Dependencies — exact version pins

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1             # passlib 1.7.4 is incompatible with bcrypt>=4.1 (breaks backend init)

# Database ORM
sqlalchemy==2.0.31
alembic==1.13.2
greenlet          # required by sqlalchemy async engine

# Database drivers
aiosqlite==0.20.0         # SQLite async
psycopg2-binary   # PostgreSQL (sync / fallback)
asyncpg          # PostgreSQL async (Supabase production)

# HTTP client
httpx==0.27.0

# File parsing
pymupdf           # PDF → fitz
python-docx==1.1.2        # DOCX
openpyxl==3.1.5           # XLSX (bonus)
python-multipart==0.0.9   # multipart form data

# Embeddings (local, free, no API needed)
sentence-transformers==3.0.1
torch          # CPU-only torch to keep size down
numpy>=2.0.0

# AI SDKs — Grok (Groq) + Gemini ONLY. No OpenAI, no Claude.
groq==0.11.0                     # Grok via Groq API (llama-3.3-70b-versatile)
google-genai==1.2.0              # Gemini — new official SDK (pinned for fast install)
google-generativeai==0.7.2       # legacy fallback (used by patient streaming chat)
reportlab==4.2.2                 # PDF export for clinical summaries

# Rate limiting
slowapi==0.1.9
limits==3.13.0

# Validation & serialization
pydantic
pydantic-settings

# Utilities
python-dateutil
bleach           # sanitize user input
python-dotenv
